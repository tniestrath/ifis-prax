import { GridCardDirective } from './grid-card.directive';

describe('GridCardDirective', () => {
  it('should create an instance', () => {
    const directive = new GridCardDirective();
    expect(directive).toBeTruthy();
  });
});
