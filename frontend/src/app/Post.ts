export class Post {
  constructor(public title : string, public date : string, public type : string, public clicks : string, public tags : string[], public performance: number, public relevance: number) {
  }
}
